pipelines:
  test-pipeline: # name of pipeline
    group: Microservices
    materials:
      servicegit: # name of material
        git: https://github.com/SamiMe/bug.git
        branch: master
        username: SamiMe
        encrypted_password: AES:w7udrFNxcLMHsaQcsVnRRw==
        destination: gitmaterialdir
#        auto_update: true
    stages:
      - print_pwd: # name of stage
          clean_workspace: true
          #approval: manual
          jobs: # jobs are ran in parallel
            pwd: # name of job
              elastic_profile_id: alpine
              tasks:
               - exec:
                   command: bash
                   arguments:
                    - "-c"
                    - "pwd && ls"
